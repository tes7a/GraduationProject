const initialLoginState: any = {

}

export const LoginReducer = (state = initialLoginState, action: ActionsTypeReducer): any => {
    switch (action.type) {
        default:
            return state
    }
}

//action

//thunk

//type

type ActionsTypeReducer = any
