const initialPassRecState: any = {

}

export const SigninReducer = (state = initialPassRecState, action: ActionsTypeReducer): any => {
    switch (action.type) {
        default:
            return state
    }
}

//action

//thunk

//type

type ActionsTypeReducer = any
